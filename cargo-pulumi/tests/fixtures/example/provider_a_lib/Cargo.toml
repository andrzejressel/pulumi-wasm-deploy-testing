[package]
name = "provider_a_lib"
version = "0.1.0"
edition = "2021"

[package.metadata.pulumi]
related_crate = "provider_a"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/referen

[dependencies]
wit-bindgen = "0.22.0"

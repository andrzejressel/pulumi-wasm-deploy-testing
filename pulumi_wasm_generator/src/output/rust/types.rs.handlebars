{{#each package.types as |type|}}

#[derive(serde::Serialize)]
pub struct {{type.struct_name}} {
    {{#each type.fields as |field|}}
    pub r#{{field.name}}: {{&field.type_}},
    {{/each}}
}

{{/each}}

{{#each package.aliases as |alias|}}
pub type {{alias.name}} = {{&alias.type_}};
{{/each}}